# Codex UI metadata and invocation policy (spec: Codex skill-creator references/openai_yaml.md).
# Triggering uses the SKILL.md frontmatter name/description, which Claude Code and Codex both read.
interface:
  display_name: "OpenFold3"
  short_description: "OpenFold3 cofolding and optimization kit, config-first"
  default_prompt: "Use $openfold3 to probe this host and plan an OpenFold3 prediction for my complex."

policy:
  allow_implicit_invocation: true
