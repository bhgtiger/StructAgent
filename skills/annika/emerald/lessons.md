# Lessons

Empty. Populated over time from real EMERALD runs — e.g. map/model framing
gotchas, weight tuning for particular map qualities, params generation
pitfalls. Use `distill-session` to append from session transcripts.
